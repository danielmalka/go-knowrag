docs
bin/
cover.out

# `make test-embedder` importa server.py, e o CPython escreve o bytecode ao lado do fonte
__pycache__/
internal/schema/applied_state.json.tmp-*

# secrets — nunca commitar num repo publico
.env
.env.*
!.env.example

# binarios compilados na raiz — `go build` sem -o deixa um com o nome do pacote
/mcp-server
/cli
/knowrag
/measure-ingest
/measure-search

# Worktrees criados por subagentes: repositórios git próprios, efêmeros, e nunca conteúdo deste.
.claude/worktrees/

# Durable reports from cmd/measure-ingest and cmd/measure-search. Generated by a real run against a
# real deployment — note counts and a timing profile of the dono's own vault/infrastructure — so it
# is never committed, same reasoning as docs/ (CLAUDE.md).
/out/
